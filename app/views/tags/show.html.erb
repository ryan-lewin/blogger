<h1>Articles Tagged with <%= @tag.name %></h1>

<ul class="list-group">
  <% @tag.articles.each do |article| %>
    <li class="list-group-item"><%= link_to article.title, article_path(article) %></li>
  <% end %>
</ul>